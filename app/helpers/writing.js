export function writeEffect(selector) {
  const typed = new Typed(selector, {
    strings: [" César González"],
    typeSpeed: 85,
    startDelay: 350,
    backSpeed: 85,
    smartBackspace: true,
    backDelay: 1000,
    loop: true,
    loopCount: false,
    showCursor: true,
    contentType: "html",
  });
}
